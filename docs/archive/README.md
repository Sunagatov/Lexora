# Archive

Files under `docs/archive/` are historical records only.

They are not active agent context, not current project guidance, and should not be used as source-of-truth unless a user explicitly asks for historical background.
