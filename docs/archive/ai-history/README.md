# AI History Archive

This folder contains old AI-maintained notes and migration records moved out of `docs/ai/` so they do not look like active agent guidance.

Use canonical current context from `AGENTS.md` and `docs/ai/*` instead.
