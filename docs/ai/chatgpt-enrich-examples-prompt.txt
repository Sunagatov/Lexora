Take this Lexora export JSON and return ONLY a valid Lexora v2 import JSON.

Goal:
- If the export includes example-completeness hints, skip words that already have 3 examples.
- For every word in the export, add exactly 3 natural English example sentences.
- Put them into example_entries as an array of exactly 3 strings.
- Each sentence must use the target word naturally.

Rules:
- Return JSON only. No markdown fences. No explanation.
- Use schema_version "lexora.ai-curation.v2".
- Keep source_topic_id from the export unchanged.
- Keep exported_at from the export unchanged.
- Use word_updates — one entry per word.
- Each entry needs only: id and example_entries.
- Do not include any other fields.
- No Russian in the examples.
- Keep examples concise, natural, practical, and vocabulary-app friendly.

Return JSON in this exact shape:
{
  "schema_version": "lexora.ai-curation.v2",
  "source_topic_id": <source_topic_id from export>,
  "exported_at": "<exported_at from export>",
  "dry_run": true,
  "word_updates": [
    {"id": 123, "example_entries": ["...", "...", "..."]},
    {"id": 456, "example_entries": ["...", "...", "..."]}
  ]
}

Here is the export JSON:
<paste the prod export JSON here>
